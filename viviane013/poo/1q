import java.util.Scanner;
    public class Quest6 {
        public static void main (String[] args) {
                Scanner s;
                double refei;
                double comissao;
                double subtotal;
                double 1calculo;
                double 2calculo;
                double 3calculo;

                s = new Scanner(System.in);

                System.out.print(" o valor da refeiçao: ");      
                refei = s.nextDouble();

                System.out.print(" o valor da comissao: ");
                comissao = s.nextDouble();
                
                2calculo = comissao / 100; 
                subtotal = refei;
                1calculo = (refei + comissao);
                3calculo = (2calculo * refei);
    
                System.out.println("Subtotal: " + subtotal + " R$ ");
                System.out.println("Total: " + 1calculo + " R$ ");
                System.out.println("Valor da comissao: " + 3calculo);
            }
    }

